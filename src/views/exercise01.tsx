import { defineComponent } from "vue";

export default defineComponent(() => {
  return () => (
    <div>
      <div class={'text-center mb-4 text-[#333]'}>exercise01 - hello world</div>
    </div>
  )
})